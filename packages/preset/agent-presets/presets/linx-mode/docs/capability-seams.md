# Extension Points And Core Services

## Service graph

- Map the interface, implementation providers, consumers, and lifecycle of each core service.
- Use a graph when several packages contribute to or consume the same extension point.

## Extension rules

- Attach new behavior to documented extension points.
- Changing a core execution path requires an architecture decision and corresponding documentation updates.
- A complete extension boundary defines its interface, provider, and consumer responsibilities.
